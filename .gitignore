node_modules/
server_data/
.claude/

# Generated build output is validated by CI and committed only in release/static-hosting updates.
dist/
docs/asset-manifest.json
data/protectiveDevices.mjs

# Local run output — never ship logs, test state, or scratch files on the public site.
*.log
tmp/
test-results/
.playwright-mcp/
